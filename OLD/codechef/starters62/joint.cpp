#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n;
    cin >> n;
    string characters;
    cin >> characters;
    int firstchar = 0, secondchar = 0;
    char first, second;
    
    int i;
    for(i = 0; true; i++){
        if(i == 0) first = characters[i];
        if(characters[i] == first) firstchar++;
        else{

        }
    }
}

int main(int argc, char*argv[]){
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    
}